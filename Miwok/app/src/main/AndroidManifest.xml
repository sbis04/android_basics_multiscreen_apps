<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.miwok">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FamilyActivity" android:label="@string/category_family"/>
        <activity android:name=".PhrasesActivity" android:label="@string/category_phrases"/>
        <activity android:name=".ColorsActivity" android:label="@string/category_colors"/>
        <activity android:name=".NumbersActivity" android:label="@string/category_numbers"/>
        <activity android:name=".MainActivity">
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.MAIN" />
                <!-- To make the app visible from Google app search results -->
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>